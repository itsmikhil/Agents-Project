[project]
name = "agents-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "langchain>=1.3.1",
    "langchain-mistralai>=1.1.4",
    "tavily>=1.1.0",
]
